% ============================================================
%  SCHOLA DOMESTICA MATHEMATICS — Early Level 1 Template
%  Weeks 1–8: 8–12 problems, no speed drill, picture-heavy
%  Usage: Copy this file, fill in \lessonnum, \lessonweek,
%         \lessontitle, \instructiontext, and problem blocks.
% ============================================================

\documentclass[12pt, letterpaper]{article}

% ── Packages ────────────────────────────────────────────────
\usepackage[top=1in, bottom=1in, left=1in, right=1in]{geometry}
\usepackage{fontspec}          % requires XeLaTeX or LuaLaTeX
\usepackage{graphicx}
\usepackage{array}
\usepackage{tabularx}
\usepackage{multicol}
\usepackage{enumitem}
\usepackage{tikz}
\usetikzlibrary{shapes, calc}
\usepackage{xcolor}
\usepackage{amssymb}
\usepackage{amsmath}
\usepackage{mdframed}
\usepackage{parskip}
\usepackage{microtype}
\usepackage{fancyhdr}
\usepackage{calc}

% ── Fonts ───────────────────────────────────────────────────
\setmainfont{EB Garamond}[
  Ligatures = TeX,
  Numbers   = OldStyle
]
\newfontfamily\titlefont{CormorantGaramond-Bold}

% ── Colors ──────────────────────────────────────────────────
\definecolor{sd-blue}{RGB}{60, 100, 160}   % header color
\definecolor{sd-gold}{RGB}{180, 140, 50}   % accent rule
\definecolor{sd-light}{RGB}{245, 242, 235} % instruction box bg
\definecolor{sd-green}{RGB}{70, 130, 80}   % knowledge check

% ── Page style ──────────────────────────────────────────────
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\headrule}{\hbox to\headwidth{%
  \color{sd-gold}\leaders\hrule height \headrulewidth\hfill}}
\fancyhead[L]{\small\color{sd-blue}\textit{Schola Domestica Mathematics}}
\fancyhead[R]{\small\color{sd-blue}\textit{Level~1 \cdot\ Week~\lessonweek\ \cdot\ Lesson~\lessonnum}}
\fancyfoot[C]{\small\color{sd-blue}\thepage}

% ── Metadata macros (fill in per lesson) ────────────────────
\newcommand{\lessonnum}{1}          % lesson number this week (1–5)
\newcommand{\lessonweek}{1}         % week number
\newcommand{\lessontitle}{Numbers 1, 2, and 3}
\newcommand{\instructiontext}{%
  Today we learn to count and write the numbers 1, 2, and 3.
  Point to each picture and count aloud with your teacher.}

% ── Reusable answer blank ────────────────────────────────────
\newcommand{\ansblank}{\underline{\hspace{1.4cm}}}

% ── Instruction box ──────────────────────────────────────────
\newmdenv[
  backgroundcolor = sd-light,
  linecolor        = sd-blue,
  linewidth        = 1pt,
  leftmargin       = 0pt,
  rightmargin      = 0pt,
  innerleftmargin  = 10pt,
  innerrightmargin = 10pt,
  innertopmargin   = 8pt,
  innerbottommargin= 8pt,
  roundcorner      = 4pt
]{instructbox}

% ── Knowledge Check box ─────────────────────────────────────
\newmdenv[
  backgroundcolor = white,
  linecolor        = sd-green,
  linewidth        = 1.5pt,
  leftmargin       = 0pt,
  rightmargin      = 0pt,
  innerleftmargin  = 10pt,
  innerrightmargin = 10pt,
  innertopmargin   = 8pt,
  innerbottommargin= 8pt,
  roundcorner      = 4pt
]{checkbox}

% ── Picture frame helper ─────────────────────────────────────
% Usage: \picframe{filename}{width}{caption}
\newcommand{\picframe}[3]{%
  \begin{center}
    \fbox{\includegraphics[width=#2]{#1}}\\[2pt]
    {\small\itshape #3}
  \end{center}}

% ── Problem counter ──────────────────────────────────────────
\newcounter{probnum}
\newcommand{\prob}{%
  \stepcounter{probnum}%
  \noindent\textbf{\arabic{probnum}.}\quad}

% ============================================================
\begin{document}

% ── Lesson Header ────────────────────────────────────────────
\begin{center}
  {\color{sd-blue}\Large\titlefont
    Schola Domestica Mathematics — Level~1}\\[4pt]
  {\color{sd-blue}\rule{\linewidth}{1.5pt}}\\[6pt]
  {\LARGE\bfseries Lesson~\lessonnum\quad|\quad\lessontitle}\\[4pt]
  {\color{sd-gold}\rule{\linewidth}{0.8pt}}
\end{center}

\vspace{6pt}

% ── Instruction Box ──────────────────────────────────────────
\begin{instructbox}
  \noindent\textbf{\color{sd-blue}Instruction} (read aloud to your student):\\[4pt]
  \instructiontext
\end{instructbox}

\vspace{10pt}

% ── Practice Problems ────────────────────────────────────────
\section*{\color{sd-blue}Practice}

% === PASTE PROBLEM BLOCKS HERE ==============================
%
%  Early lesson problems are grouped:
%  1) Picture-counting problems (images)
%  2) Trace-and-write numerals
%  3) Simple matching or circling
%  4) Oral story problem (teacher reads aloud)
%
% Use the \prob macro for each numbered item.
% Use \picframe{filename}{width}{caption} for images.
% Use \ansblank for write-in answers.
%
% ============================================================

\vspace{16pt}
{\color{sd-gold}\rule{\linewidth}{0.6pt}}
\vspace{6pt}

% ── Knowledge Check ──────────────────────────────────────────
\begin{checkbox}
  \noindent\textbf{\color{sd-green}Knowledge Check}\\[4pt]
  % === INSERT 1–2 quick check questions here ================
  \prob How many do you see? \quad
    \includegraphics[height=1.2cm]{stars_3.jpg}\quad
    \ansblank\\[10pt]
  \prob Write the number three: \quad \ansblank
\end{checkbox}

\vspace{10pt}

% ── Enrichment (optional) ────────────────────────────────────
% Uncomment and fill in when appropriate
% \noindent\textbf{\color{sd-blue}$\star$ Enrichment:} ...

% ── Teacher Notes (printed small at bottom) ─────────────────
\vfill
{\footnotesize\color{gray}\textit{Teacher note: Use real objects (beans, buttons, coins)
to reinforce counting before or after this lesson.
Mastery target: student correctly identifies and writes each
numeral without hesitation.}}

\end{document}
